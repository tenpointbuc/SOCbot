# Ansible Galaxy collections required by the bundle.
# Install:  ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: community.docker
    version: ">=3.10.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
